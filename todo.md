1. Look into template repositories.
2. private repos, authentication with ssh or https
3. make conda env (or pyproject.toml) or something so that the python environment is reproducible (nix if its possible?).